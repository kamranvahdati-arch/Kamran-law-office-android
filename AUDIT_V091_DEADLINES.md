# الحاقیه مواعد نسخه ۹.۱ — ممیزی پیش از تغییر

مبنا: commit محلی `d440bdf`؛ این سند تأیید آمادگی انتشار نیست. هیچ نتیجه تست اجرایی از صرف خواندن سورس استنتاج نشده است.

| بند | وضعیت | شاهد سورس و اقدام لازم |
|---|---|---|
| ۱ جلسه دادگاه مستقل و اطلاعات کامل | Partial | `OfficeDb.appointments` و `MainActivity.addAppointment` نوع جلسه دارند؛ شعبه/مجتمع/شهر و وضعیت حضور فیلد مستقل ندارند. پایان فعلاً اجباری است. |
| ۲ انواع مهلت قابل ویرایش | Partial | `addDeadline` شش نوع ثابت دارد؛ تنظیمات انواع مهلت و فهرست کامل لازم است. |
| ۳ اتصال به پرونده و موکل و نمای متمرکز | Partial | مهلت به پرونده وصل است، موکل از موکل اصلی استخراج می‌شود؛ انتخاب صریح موکل و نمای مشترک جلسات/مهلت لازم است. |
| ۴ وضعیت و تاریخ‌ها | Partial | شروع/پایان/مدت و completed وجود دارد؛ لغو و زمان تکمیل مستقل نیست. انقضا باید از تاریخ محاسبه شود. |
| ۵ سطوح ۳،۲،۱،۰ روز | Partial | `deadlineCard` روزشمار دارد؛ سطوح هشدار مشترک جلسات و مهلت و آزمون تم لازم است. |
| ۶ یادآوری قابل تنظیم | Partial | مهلت ۷،۳،۱،۰ و جلسه ۱،۰ ثابت؛ ساعت مهلت ۹ ثابت. یادآوری دو روزه، ساعتی، صبح و ویرایش مستقل وجود ندارد. |
| ۷ آمادگی جلسه | Missing | مدل/فرم یادآوری آماده‌سازی و مرور دفاعیات وجود ندارد. |
| ۸ عقب‌افتاده | Partial | مهلت باز گذشته در داشبورد هست ولی فقط شش مورد نمایش داده می‌شود؛ جلسه گذشته تعیین‌تکلیف‌نشده وضعیت ندارد. |
| ۹ تکمیل و زمان انجام | Partial | `completeDeadline` فقط completed/updated_at می‌نویسد؛ جلسه مسیر تکمیل ندارد. |
| ۱۰ چهار بخش اولویت‌دار | Partial | جلسات امروز و مهلت نزدیک/گذشته هست؛ مهلت امروز جدا نیست و بخش‌ها زیر آمار/دسترسی سریع‌اند. |
| ۱۱ محتوای کارت | Partial | عنوان/پرونده/موکل/تاریخ برای مهلت هست؛ وضعیت کامل و شعبه جلسه کم است. |
| ۱۲ تاریخ/ساعت دستی و picker | Partial | helperهای مشترک متصل‌اند؛ آزمون تعاملی کامل انجام نشده است. |
| ۱۳ زمان و شمارش | Partial | `JalaliDate` برای حساب روز از timezone پیش‌فرض و اختلاف میلی‌ثانیه استفاده می‌کند؛ حساب روز باید UTC مستقل از منطقه دستگاه باشد. زمان واقعی اعلان باید جدا و دارای timezone مشخص باشد. |
| ۱۴ مهاجرت | Partial | تغییرات الحاقیه هنوز migration ندارند؛ حفظ تک‌تک تاریخ‌ها/شناسه‌ها و سازگاری backup باید آزمایش شود. |
| ۱۵ تست‌های عملی | Missing | تست ساده تاریخ موجود است؛ ماتریس جدید، تغییر موعد، تکمیل، اعلان تکراری و تم‌ها هنوز اجرا نشده‌اند. |

## ایرادات قطعی مشاهده‌شده

- `ReminderReceiver` پیش از کنترل permission، یادآوری را fired علامت می‌زند؛ اعلان ممنوع می‌تواند یادآوری را مصرف کند.
- `pendingReminders` فقط زمان آینده را برمی‌گرداند؛ بازیابی یادآوری از دست‌رفته پس از خاموشی/توقف نیازمند سیاست catch-up و محدودیت ضداسپم است.
- `setAndAllowWhileIdle` تضمین ارسال دقیق رأس ساعت ندارد؛ نمایش ساعت قطعی نباید وعده تضمینی ایجاد کند.
- تاریخ‌های ذخیره‌شده شمسی حفظ می‌شوند؛ اصلاح حساب روز نباید epoch اعلان‌های قبلی را خودکار تغییر دهد.

## ترتیب اصلاح

۱. استقلال حساب تقویمی از timezone و گسترش تست تاریخ؛ ۲. اصلاح مصرف اعلان بدون permission؛ ۳. migration افزایشی وضعیت/حضور/موکل/مکان و قواعد reminder؛ ۴. فرم و داشبورد و گزارش؛ ۵. تست ارتقا و شبیه‌ساز و ساخت APK.

وضعیت فعلی: مرحله‌های ۳ تا ۵ تکمیل نشده‌اند. نسخه قابل انتشار/تأییدشده تحویل نشده است.

## پیشرفت قابل اثبات این مرحله

- حساب روز و طول اسفند در `JalaliDate` به تقویم مدنی UTC منتقل شد. تاریخ‌های ذخیره‌شده و زمان اعلان‌های موجود تغییر نکردند. این اصلاح، سیاست timezone جلسات را تعیین نمی‌کند.
- بررسی مجوز سیستم، فعال بودن اعلان برنامه و فعال بودن کانال به قبل از `fireReminder` منتقل شد. آزمون عملی Android این تغییر هنوز اجرا نشده است؛ catch-up همچنان باقی است.
- تست‌های Java واقعاً اجرا شدند: `DeadlineDateSmokeTest` و `InputValidatorsSmokeTest` هر دو موفق. سناریوهای شمارش ۱۰،۳،۲،۱،۰،منفی۱،منفی۱۰ روز در UTC، تهران، نیویورک، آپیا و برلین؛ مرز سال، اسفند کبیسه و تاریخ حذف‌شده محلی آپیا پوشش داده شدند.
- ابزار compiler از ماژول `jdk.compiler` اجرا شد؛ Gradle، Android SDK و emulator محلی در دسترس نبودند. موفقیت تست Java به معنی موفقیت Build یا تست اعلان و migration Android نیست.

## تغییرات افزایشی بعدی — هنوز تأیید نهایی نشده

- schema 12: افزودن شعبه/مرجع/شهر/وضعیت حضور/زمان تکمیل جلسه، موکل و زمان تکمیل مهلت، عنوان یادآوری؛ بدون بازنویسی تاریخ‌ها و epochهای قبلی.
- چهار بخش اصلی مواعد به بالای آمار کم‌اولویت منتقل شدند؛ محدودیت مخفی‌کننده شش مهلت گذشته برداشته شد؛ جلسات گذشته فعال نیز قابل مشاهده‌اند.
- وضعیت انجام/لغو/فعال‌سازی، تغییر موعد با هشدار بازتنظیم یادآوری، ثبت زمان انجام؛ پایان جلسه اختیاری شد.
- انواع مهلت در تنظیمات قابل ویرایش و داخل backup تنظیمات هستند.
- پیش‌فرض یادآوری موعدهای جدید ۷،۳،۲،۱،۰ روز؛ افزودن یادآوری با تاریخ/دقیقه دلخواه و عنوان آماده‌سازی/مرور دفاعیات؛ غیرفعال‌سازی هر یادآوری و جلوگیری از ثبت ساعت تکراری.
- تست `ScheduleMigrationTest` افزوده شده ولی نتیجه آن تا اجرای CI مشخص نیست.
- ساخت و شبیه‌ساز Actions run `35506199813` برای checkpoint محدود `7765929` موفق بود؛ این نتیجه به تغییرات بعدی schema 12 تعمیم داده نمی‌شود.

### موارد باقیمانده

سیاست timezone رویداد هنگام سفر، catch-up ضداسپم پس از خاموشی، وضعیت عدم حضور در UI، نمایش کامل زمان انجام در گزارش، تغییر نوع/موکل موعد در ویرایش کامل، تأیید عملی اعلان و تمام تم‌ها هنوز نیازمند تکمیل/تست‌اند. UI ثبت اطلاعات تکمیلی جلسه و موکل مهلت چند نوشتن پیاپی دارد و باید اتمیک شود. سایر بندهای ممیزی اصلی ۹.۱ نیز همچنان معتبرند. این سند ادعای پایان نسخه ۹.۱ نیست.

## پیگیری بعدی

- Actions run 42، commit راه‌دور `a9099c8`: ساخت APK و شش آزمون شبیه‌ساز روی API 30 و API 35 موفق شدند. شکست run 41 انتظار قدیمی چهار reminder بود؛ پیش‌فرض جدید پنج مرحله است و انتظار آزمون اصلاح شد.
- ذخیره جزئیات جلسه و موکل مهلت اکنون در تراکنش واحد انجام می‌شود.
- catch-up فقط آخرین یادآوری ازدست‌رفته هر موعد در ۲۴ ساعت گذشته را انتخاب می‌کند؛ ارسال مراحل قدیمی پشت‌سرهم پس از بازگشت کاربر انجام نمی‌شود. این تغییر جدید هنوز نیازمند اجرای تست receiver است.
- schema 13 به‌طور افزایشی ارتباط چک با ثبت وصول در ledger را اضافه می‌کند؛ چک‌های مرتبط قبلی از installment_payments تطبیق داده می‌شوند. وصول/برگشت و مانده قسط اتمیک می‌شوند. داده مالی مبهم قدیمی خودکار به دریافت جدید تبدیل نمی‌شود.
- حذف دمو، والدهای لازم برای داده ثبت‌شده کاربر را حفظ می‌کند؛ گزارش مالی جمع مبالغ را از همان ردیف‌های فیلترشده محاسبه می‌کند.
- تست‌های جدید CheckAccountingTest، DemoIsolationTest، FinancialFilterTest و ReminderDeliveryTest اضافه شده‌اند؛ تا اجرای CI موفق اعلام نمی‌شوند.
