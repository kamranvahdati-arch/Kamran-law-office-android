# ممیزی مبنای نسخه حرفه‌ای اندروید

مبنای این ممیزی شاخه `main` و commit `b2233c3b56b02fd4863f9c34210c993b2a953980` است. ممیزی پیش از migration یا بازطراحی گسترده انجام شد. مخزن وب‌سایت، دامنه، DNS، Production، Windows و iOS خارج از دامنه این کار هستند.

## وضعیت واقعی موجود

| حوزه | موجود و قابل حفظ | نقص یا ریسک تأییدشده |
| --- | --- | --- |
| معماری | اپ native Java، حداقل Android 7، RTL، ساخت GitHub Actions | تقریباً تمام UI و navigation در `MainActivity.java` و تمام داده در `OfficeDb.java` متمرکز است؛ تغییرات بزرگ در این دو فایل ریسک regression دارد |
| امنیت داده | SQLCipher، کلید تصادفی بسته‌بندی‌شده با Android Keystore، قفل PBKDF2، biometric اختیاری، `allowBackup=false`، بکاپ AES-GCM | Release با debug signing ساخته می‌شود؛ پروفایل/تنظیمات/تصاویر داخل بکاپ دیتابیس نیستند؛ تست گوشی واقعی ثبت نشده است |
| migration | انتقال دیتابیس قدیمی بی‌رمز به دیتابیس v9، مقایسه تعداد رکوردها، حذف فایل قدیمی پس از تأیید | migration برگشتی خودکار ندارد؛ تست فعلی فقط یک سناریوی محدود و API 30 را پوشش می‌دهد |
| همگام‌سازی آینده | بیشتر جدول‌ها `uid`، `created_at`، `updated_at` و `deleted_at` دارند | موتور sync، revision/server version، حل تعارض و صف تغییرات وجود ندارد |
| داده دمو | ۱۰۰ موکل، پرونده، کار، تراکنش و اقدام نمونه برای تست نصب تازه ایجاد می‌شود | ستون یا metadata قابل‌اتکایی برای تشخیص دمو وجود ندارد؛ حذف فقط داده‌های نمونه ممکن نیست؛ seed اجباری برای نسخه روزمره مناسب نیست |
| UI/UX | فارسی، RTL، فونت قابل تنظیم، حالت روشن/تیره، منوی سه‌نقطه، inset ابتدایی | رنگ‌ها و ابعاد در Activity hard-code شده‌اند؛ فقط دو تم؛ تقویم در حالت تیره از `Color.WHITE` استفاده می‌کند؛ آیکون‌ها عمدتاً نویسه/emoji هستند؛ تست اندازه صفحه و status/navigation bar وجود ندارد |
| داشبورد | آمار جاری/مختومه/موکل/کار، عقب‌افتاده، نیازمند اقدام، جلسه امروز، مهلت، تقویم | تعداد کل پرونده‌ها جداگانه نیست؛ نام روز هفته کامل نمایش داده نمی‌شود؛ عکس و لوگو ناخواسته پروفایل را باز می‌کنند؛ عنوان محصول در میز کار نیست |
| پروفایل وکیل | نام، پایه، شماره پروانه، نهاد آزادمتن، تلفن، آدرس، وب‌سایت، عکس و لوگوی دستی | نهاد ساختاریافته، استان/شهر وابسته، کد ملی و validation، موبایل اجباری، OTP/readiness و لوگوی رسمی داخلی وجود ندارد |
| موکل و پرونده | ثبت/ویرایش، جاری/مختومه، جست‌وجو، soft-delete، اتصال کار/قرار/مهلت و گزارش | هر پرونده فقط یک `client_id` دارد؛ رابطه چندموکله، Trash/restore و confirmation دومرحله‌ای کامل وجود ندارد |
| قرارداد و همکاری | شماره/تاریخ/یادداشت قرارداد و حق‌الوکاله روی پرونده | قرارداد الکترونیک مشترک، اختیارات، طرف‌های مقابل، حق توکیل، همکار وکیل، چندوکیله بودن و سهم مالی وجود ندارد |
| حسابداری | ledger، حق‌الوکاله، هزینه‌ها، پرداخت‌کننده، اقساط و پرداخت قسط، صورت‌حساب قابل چاپ | قرارداد مالی مستقل، روش پرداخت، چک و وضعیت وصول، سهم همکار، فیلتر جامع مالی و سررسید چک وجود ندارد |
| تاریخ و ساعت | parser شمسی و اعتبارسنجی دستی، ساعت ۲۴ساعته دستی | DatePicker شمسی و TimePicker وجود ندارد؛ همه فرم‌ها تجربه یکسان ندارند |
| گزارش | پرونده/موکل، گزارش مالی کلی، عملکرد و PDF از مسیر Print Framework | گزارش مالی و عملکرد فاقد فیلترهای کامل بازه/نوع/دسته/سررسید هستند |
| برنامه روزانه | کار مستقل یا متصل، زمان شروع/پایان، قرار و ملاقات، تقویم | timeline روزانه، دسته‌های شخصی قابل‌ویرایش و کنترل تداخل زمانی وجود ندارد |
| اعلان | یادآوری چندمرحله‌ای مهلت، کار و قرار؛ درخواست permission Android 13+ | تنظیم داخلی روشن/خاموش، اقساط/چک، جلوگیری جامع از تکرار، deep-link دقیق و قرارداد اعلان مدیریتی وجود ندارد |
| تماس/درباره/تجاری | نسخه در تنظیمات نمایش داده می‌شود | صفحه تماس، درباره/اعتبار، update manifest امن، Trial/entitlement، account verification state، Privacy consent و public-profile opt-in وجود ندارد |
| قوانین و آرا | وجود ندارد | cache/version/search آفلاین و قرارداد API با Source of Truth وب‌سایت باید افزوده شود؛ scraping داخل اپ ممنوع می‌ماند |
| آزمون و CI | smoke test تقویم، instrumentation برای encryption/migration/backup، build و انتشار APK/ZIP | پوشش validation، چندموکله، قرارداد، همکار، چک، فیلتر، Trash، theme/RTL/inset/offline و migration v9→نسخه بعد وجود ندارد |

## تصمیم معماری پیشنهادی پس از ممیزی

1. قابلیت‌های سالم v9 حفظ شوند و migration افزایشی v10/v11 نوشته شود؛ دیتابیس بازسازی یا پاک نشود.
2. پیش از توسعه UI گسترده، validationها، Theme tokens، تنظیمات و مدل‌های جدید به کلاس‌های کوچک مستقل منتقل شوند؛ `MainActivity` یکباره بازنویسی نشود.
3. رابطه قدیمی `cases.client_id` در migration حفظ شود، ولی منبع جدید ارتباط `case_clients` باشد و برای داده‌های قبلی خودکار backfill شود.
4. قرارداد الکترونیک، همکاران، قرارداد مالی، چک، taxonomy، تنظیمات backend و داده حقوقی cache شده در جدول‌های مستقل و sync-ready ذخیره شوند.
5. همه رکوردهای دمو با `is_demo` یا رجیستری seed علامت‌گذاری شوند و حذف دمو فقط همان رکوردها را soft-delete کند؛ داده موجود خودکار حذف نشود.
6. قابلیت‌های وابسته به سرور فقط با model، API contract، feature flag و حالت «در دسترس نیست» آماده شوند؛ هیچ OTP، لایسنس، پرداخت، اعلان مدیریتی یا احراز هویت جعلی پیاده نشود.
7. APK این مرحله فقط آزمایشی است تا زمانی که keystore تولید امن خارج از repository، تست گوشی واقعی و backend معتبر فراهم شود.

## ترتیب کم‌ریسک اجرا

1. زیرساخت تست، validation، Theme و تنظیمات ایمن.
2. migration افزایشی و مدل‌های چندموکله/قرارداد/همکار/مالی/دمو.
3. UI پروفایل، پرونده و قرارداد و سپس گزارش‌ها و برنامه روزانه.
4. اعلان‌ها، Trash، تماس/درباره، قراردادهای backend و قوانین آفلاین.
5. آزمون migration/backup/restore، build Release آزمایشی و انتشار APK/ZIP.
