# ممیزی نسخه ۰٫۸٫۰ و مسیر نسخه هشت

این ممیزی از `MainActivity.java`، `OfficeDb.java`، `SecureBackup.java`، `JalaliDate.java`، مانیفست و workflow ساخت تهیه شد؛ پیش از تغییر سورس انجام شد. وضعیت اجرای واقعی روی گوشی و بازیابی با داده‌های واقعی، از خواندن سورس قابل اثبات نیست.

| بخش | آنچه واقعاً در ۰٫۸٫۰ وجود داشت | نقص یا خطر |
| --- | --- | --- |
| پرونده و موکل | ثبت، فهرست، جستجو، فیلتر، ارتباط موکل و تغییر جاری/مختومه | ویرایش/حذف در رابط نبود؛ اتصال کار فقط رشته آزاد بود |
| داشبورد | آمار، تقویم شمسی، کار فوری امروز، ملاقات‌ها، شمارش مهلت | کار عقب‌افتاده و پرونده نیازمند اقدام به‌صورت مشخص تفکیک نمی‌شد |
| کار و قرار | ثبت کار با یک ساعت، ملاقات با شروع/پایان و ارتباط اختیاری | نوع ملاقات حضوری/تلفنی و مرجع کامل نبود؛ شروع/پایان کار و اتصال واقعی پرونده و موکل نبود |
| مهلت | ثبت تاریخ رویداد، مدت و تاریخ نهاییِ تأییدشده به دست وکیل، نمایش روزشمار | اعلان سیستم و چند مرحله یادآوری نبود؛ محاسبه فقط افزودن روز تقویمی است و قاعده قضایی را تعیین نمی‌کند |
| مالی | حق‌الوکاله توافقی، دفتر دریافت/هزینه با پرداخت‌کننده، جمع مانده و چاپ PDF | قرارداد ساختاریافته و اقساط/سررسید/پرداخت قسط نبود؛ هزینه پرداختی موکل از بدهی وکیل جداگانه جمع‌بندی می‌شد |
| بکاپ | فایل `.klo` با AES-GCM و PBKDF2؛ بازیابی در تراکنش | نبودن جدول در بکاپ باعث حذف داده‌های حاضر می‌شد؛ تصاویر، پروفایل و تنظیمات در بکاپ نبود؛ اعتبار کل ارجاعات پیش از commit بررسی نمی‌شد |
| امنیت | قفل اختیاری بر اساس SHA-256 و بکاپ رمزدار | دیتابیس اصلی SQLite بدون رمز؛ بیومتریک نبود؛ `allowBackup=true` برای داده حساس؛ رمز کوتاه و بدون نمک؛ امضای انتشار debug وابسته به runner |
| ساخت | APK توسط GitHub Actions | workflow فقط artifact با انقضای ۳۰ روز می‌سازد؛ امضای debug در هر runner ممکن است متفاوت شود و نصب نسخه جدید روی نسخه قبلی را مختل کند |

تغییرات افزوده‌شده در ۰٫۸٫۱: مهاجرت داده‌های قدیمی به دیتابیس رمزگذاری‌شده SQLCipher با کلید تصادفی و بسته‌بندی آن در Android Keystore، UID یکتا و تاریخ ویرایش/حذف نرم برای جدول‌های قدیمی، اتصال پرونده و موکل به کار، اقساط و پیوند پرداخت قسط به تراکنش مالی، شمارش کار عقب‌افتاده، یادآوری مهلت در ۷، ۳، ۱ و ۰ روز پیش از سررسید، بستن OS Auto Backup و بیومتریک اختیاری بر پایه قفل برنامه. فایل دیتابیس قدیمی بی‌رمز فقط **بعد از بازیابی و مقایسه تعداد رکوردها** حذف می‌شود. رمزنگاری و مهاجرت هنوز روی گوشی واقعی آزمایش نشده است.

## کارهای هنوز ضروری پیش از استفاده با اطلاعات واقعی

1. مهاجرت دیتابیس قدیمی به SQLCipher، بازگشت از فایل `.klo` و امکان باز کردن چندباره دیتابیس را با داده نمونه و سپس با بکاپ معتبر روی گوشی واقعی بیازماییم. ساخت APK به تنهایی این رفتار را اثبات نمی‌کند.
2. کلید امضای پایدار و محافظت‌شده برای نسخه‌های قابل به‌روزرسانی فراهم کنیم؛ امضای debug فعلی برای ارتقای قابل اعتماد مناسب نیست.
3. مسیرهای ویرایش/حذف و گزارش مالی را با رکوردهای متنوع، اقساط سررسید گذشته، پرداخت جزئی/اضافی، پرونده بدون موکل، فعال/مختومه و UIDهای تکراری آزمون کنیم؛ گزارش مالی باید قبل از امضا به‌صورت موردی با موکل تطبیق داده شود.
4. بکاپ پروفایل و تصاویر و تنظیمات و خروجی‌های استاندارد قابل همگام‌سازی، سیاست تعارض داده‌ها، قواعد sync برای tombstone، اعلان‌ها بعد از ری‌استارت و رفتارسنجی اندروید ۱۳ تا ۱۵ تکمیل شوند.

در این نسخه «آماده برای سینک» فقط به معنای داشتن UID و زمان‌های لازم است. موتور همگام‌سازی و حل تعارض هنوز وجود ندارد. پیشنهاد تقویمی مهلت نیز به معنای تعیین مهلت حقوقی نیست؛ تاریخ نهایی را وکیل ثبت می‌کند.
