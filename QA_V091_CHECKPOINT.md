# وضعیت اثبات و کار باقی‌مانده ۹.۱

این گزارش میان‌مرحله‌ای است؛ جایگزین ماتریس نهایی قبل/بعد و تأیید انتشار نیست.

## آخرین نسخه قابل بازبینی

سورس `6adc679dacc8959d8b48e770684c119728b018b4` در اجرای Actions شماره `35558142948` روی API 30 و 35 موفق بود: ساخت release و preview، دو آزمون Java، ۱۵ آزمون instrumentation و تکرار مستقل UiFlow روی اندازه 720×1280. APK preview در هر دو شبیه‌ساز نصب و اجرای Activity با وضعیت `ok` ثبت شد. آزمون دستگاه فیزیکی انجام نشده است.

برای هر API ده تصویر dashboard/settings از پنج تم ثبت شد. نمونه تصاویر بازبینی شدند؛ این پوشش، بازبینی تمام dialogها یا نوار سیستم نیست. تصویر root شامل نوار سیستم واقعی نمی‌شود. SHA-256 فایل APK قابل بازبینی: `1990321378803b7077050ecda3baa1a6da9c5e9157dd887aeb20b894ae130fe0`.

ویرایش مشخصات جلسه و مهلت، حفظ reminder در ویرایش غیرزمانی و بازسازی reminder پس از تغییر موعد به آزمون‌ها اضافه شده‌اند. Receiver سیستمی فقط حین instrumentation برای جلوگیری از تداخل با بازسازی دیتابیس fixture غیرفعال و پس از آن بازیابی می‌شود؛ تست receiver مستقیم اجرا می‌شود. این نتیجه تضمین تحویل واقعی alarm پس از reboot نیست.

## اصلاحات پس از این checkpoint

- عنوان محصول در header بالای «میز کار امروز» قرار گرفت؛ عنوان تکراری بدنه حذف شد.
- safe area حاشیه چپ و راست سیستم را هم رعایت می‌کند؛ آزمون inset چهارطرفه و ترتیب عنوان به UiContract اضافه شد.
- توضیح UI بکاپ صریح شد: خود فایل‌های عکس/پیوست منتقل نمی‌شوند و URI ذخیره‌شده دسترسی در گوشی دیگر ایجاد نمی‌کند.
- این تغییرات migration ندارند. نتیجه CI سورس checkpoint بالا نباید به این تغییرات جدید تعمیم داده شود؛ اجرای جدید لازم است.

## نتایج مشاهده‌شده

| سورس / اجرای Actions | نتیجه واقعی | محدودیت |
| --- | --- | --- |
| `a9099c8` / 35524078490 | ساخت و ۶ آزمون Android روی API 30 و 35 موفق | فقط همان checkpoint |
| `63ccff7` / 35524414610 | هر دو job موفق؛ تست وصول/برگشت چک، حفظ داده واقعی هنگام حذف دمو، گزارش فیلترشده و receiver اضافه شده‌اند | آزمون receiver معادل تضمین تحویل AlarmManager در تمام دستگاه‌ها نیست |
| `83f28e7` / 35524776508 | ساخت APK موفق؛ API 30 از ۱۲ آزمون، ۱۱ موفق و UiFlow ناموفق؛ API 35 آزمون کامل موفق، تکرار UiFlow در اندازه کوچک ناموفق | انتشار متوقف؛ شکست تست را موفق محسوب نمی‌کنیم |
| اصلاح بعدی UiFlow | انتظار محدود برای دسترس‌پذیری پنجره پس از بازکردن dialog | رفع قطعی فقط پس از اجرای مجدد قابل اعلام است |

گزارش XML API 30، خط ۳۹ UiFlowSmokeTest را نشان می‌دهد: پنجره فعال بلافاصله پس از بازکردن فرم در Accessibility هنوز در دسترس نبود. انتظار حداکثر پنج ثانیه با پیام نام dialog اضافه شد؛ assertion حذف نشده است.

## اصلاحات جدید این مرحله

- موکلان و همکاران قرارداد مستقل و چندانتخابی شدند. قرارداد همچنان یک رکورد مشترک دارد. اتصال موکل انتخابی به پرونده داخل همان تراکنش انجام می‌شود.
- ویرایش سهم/نحوه همکاری شناسه ارتباط را حفظ می‌کند؛ درصد نامعتبر و NaN رد می‌شوند؛ ثبت همکار و اتصال او اتمیک است.
- قطع ارتباط همکار با پرونده با تأیید و حذف نرم انجام می‌شود؛ قراردادهای گذشته تغییر نمی‌کنند.
- variant آزمایشی `preview` با شناسه `ir.kamranvahdati.lawoffice.preview` اضافه شد تا کنار نسخه اصلی نصب شود. این نسخه امضای توسعه دارد، production نیست و خودکار داده نسخه اصلی را نمی‌خواند.
- CI باید APK همین variant را نیز نصب و اجرا کند. موفقیت assemble به‌تنهایی اثبات نصب/اجرا نیست.
- به تاریخ نامعتبر قدیمی جلسه برچسب «تاریخ نیازمند بررسی» داده می‌شود؛ تاریخ داده کاربر خودکار تغییر نمی‌کند.

## اصلاح برداشت از ماتریس اولیه

Completeهای سند اولیه به‌معنای گواهی امنیت یا آزمون جامع تمام مسیرهای اجرایی نیستند. هر مورد فاقد آزمون متناسب باید در ماتریس نهایی Partial بماند. به‌ویژه حذف دمو در نسخه قبلی با وجود گزینه UI، مشکل حفظ وابستگی داده واقعی داشت و اصلاح شد. عکس پروفایل کاربر نباید عکس پدیدآورنده تلقی شود؛ تا asset تأییدشده پدیدآورنده موجود نباشد محل تصویر خالی می‌ماند.

## موارد باز واقعی

- ماتریس نهایی تمام زیرالزامات و آزمون تعاملی تمام فرم‌ها هنوز پایان نیافته است.
- دیتاست استان‌ها ۳۱ استان دارد، اما فهرست شهرها فعلاً پوشش مراکز شهرستان‌ها دارد؛ نباید فهرست کامل شهرهای ایران اعلام شود.
- ویرایش کامل جزئیات موعد/جلسه و برخی قراردادها هنوز کامل نیست.
- backup پایگاه داده و تنظیمات مجاز، فایل باینری پیوست‌ها و عکس‌ها را منتقل نمی‌کند. انتقال URI به گوشی دیگر تضمین دسترسی نیست.
- بازیابی تنظیمات و دیتابیس هنوز دو مخزن ذخیره‌سازی مجزا دارد؛ آزمون شکست بین این دو مرحله لازم است.
- سیاست زمان جلسه در سفر بین timezoneها، deep-link اعلان به رکورد، تحویل واقعی alarm پس از reboot و محدودیت باتری نیازمند بررسی بیشترند.
- آزمون migration فعلی شبیه‌سازی schema است؛ نصب APK تاریخی با امضای سازگار و ارتقای واقعی روی داده شبیه‌سازی‌شده هنوز اثبات نشده است.
- تصاویر رابط تمام تم‌ها، فرم‌ها، dropdownها و نوارهای سیستم هنوز بازبینی بصری کامل نشده‌اند.
- داده نمونه پنج حوزه اصلی وجود دارد؛ ۱۰۰ نمونه مستقل در تمام موجودیت‌های جدید ادعا نمی‌شود.
- PIN/biometric، قفل background و بازیابی امن نیازمند regression تکمیلی‌اند؛ وجود SQLCipher گواهی امنیت کامل نیست.
- هیچ کلید production در پروژه نیست. امضای توسعه CI ممکن است میان اجراها تغییر کند؛ حذف نسخه اصلی برای نصب آزمایشی توصیه نمی‌شود.

زیرساخت آینده لایسنس، پرداخت، احراز آنلاین، اعلان مدیریتی، بانک قوانین متصل به API سایت و پروفایل عمومی همچنان Future-Hidden است. backend واقعی اضافه یا شبیه‌سازی نشده است. وب‌سایت، DNS و نسخه‌های iOS/Windows تغییر نکرده‌اند.
