# الحاقیه مواعد — وضعیت میان‌مرحله‌ای پس از اصلاحات

مبنای قبل: `AUDIT_V091_DEADLINES.md`. این جدول جایگزین تأیید نهایی نسخه نیست. هر مسیر فاقد اثبات کافی Partial مانده است.

| بند | قبل | اکنون | شاهد و باقیمانده |
| --- | --- | --- | --- |
| ۱ جلسه مستقل و اطلاعات کامل | Partial | Partial | `saveAppointment` و فرم ثبت شامل زمان، شعبه، مرجع، شهر، محل و اشخاص؛ ویرایش مشخصات اضافه شده، آزمون جدید هنوز اجرا نشده |
| ۲ انواع مهلت قابل افزودن | Partial | Partial | `deadline_types` در تنظیمات و backup؛ مسیر عملی افزودن و استفاده به تست تعاملی نیاز دارد |
| ۳ ارتباط پرونده/موکل و نمای متمرکز | Partial | Partial | `setDeadlineClient` و `deadlineList`؛ ویرایش ارتباط جلسه اضافه شده، آزمون تعامل تمام مسیرها باقی است |
| ۴ شروع/پایان/وضعیت/روزشمار | Partial | Partial | انجام، لغو و فعال‌سازی و وضعیت منقضی محاسباتی وجود دارند؛ ویرایش تمام جزئیات مهلت باقی است |
| ۵ هشدار ۳،۲،۱،۰ روز | Partial | Partial | `countdownBadge` و حاشیه برجسته مرکزی، بدون چشمک‌زدن؛ محاسبه آزموده شده، تصاویر تمام وضعیت‌ها باید بررسی شوند |
| ۶ reminder چندمرحله‌ای قابل تنظیم | Partial | Partial | پیش‌فرض ۷،۳،۲،۱،۰؛ افزودن تاریخ/ساعت دلخواه و حذف؛ آزمون DB و receiver موفق، تحویل واقعی زمان‌بندی‌شده در همه حالت‌های سیستم آزموده نشده |
| ۷ یادآوری آماده‌سازی جلسه | Missing | Partial | عنوان و زمان سفارشی آماده‌سازی/مرور؛ تست حفظ یادآوری هنگام ویرایش تازه اضافه شده |
| ۸ نمایش موارد عقب‌افتاده | Partial | Partial | حذف محدودیت شش رکورد، جلسه گذشته فعال در داشبورد؛ بازبینی بصری وضعیت‌های پرتعداد باقی است |
| ۹ تکمیل و زمان انجام | Partial | Partial | `completed_at` و گزارش؛ تست تغییر وضعیت موفق، پوشش همه خروجی‌های PDF باقی است |
| ۱۰ چهار بخش اولویت‌دار داشبورد | Partial | Partial | جلسات امروز، مهلت امروز، نزدیک و عقب‌افتاده بالاتر از آمار؛ بازبینی تصاویر باقی است |
| ۱۱ محتوای کارت | Partial | Partial | موکل/پرونده/تاریخ/وضعیت و شعبه/ساعت جلسه؛ تاریخ نامعتبر قدیمی بدون تغییر داده علامت‌گذاری می‌شود |
| ۱۲ تاریخ و ساعت دو روش | Partial | Partial | helper مشترک دستی/picker متصل؛ smoke بازشدن picker موفق، ثبت و ویرایش با هر دو روش در همه فرم‌ها هنوز آزموده نشده |
| ۱۳ شمارش مستقل از timezone | Partial | Complete | `DeadlineDateSmokeTest`: ۱۰،۳،۲،۱،۰ و عقب‌افتاده در پنج منطقه زمانی، مرز سال/کبیسه؛ این Complete فقط حساب روز مدنی است، نه سیاست زمان جلسه هنگام سفر |
| ۱۴ migration بدون جابه‌جایی موعد | Partial | Partial | schema 12 افزایشی و تست حفظ تاریخ/epoch موفق؛ ارتقای APK تاریخی با امضای سازگار هنوز انجام نشده |
| ۱۵ تست خودکار و عملی کامل | Missing | Partial | آزمون schema، وضعیت، date change، duplicate و permission اجرا شده؛ آزمون alarm واقعی/reboot، تمام سناریوهای UI و تصاویر کامل باقی است |

## نتایج واقعی جدید

سورس `4686bc55e53413ad1809a56c55f2c27a3dbe7258`، اجرای Actions `35540601965` و `35540611847`:

- ساخت موفق است.
- در اجرای `35540601965` روی API 35، ۱۳ آزمون کامل و تکرار یک آزمون UI در اندازه کوچک با `BUILD SUCCESSFUL` پایان یافتند.
- در اجرای `35540611847` روی API 30، ۱۳ آزمون کامل و تکرار یک آزمون UI موفق شدند.
- workflow همچنان **ناموفق** است: مرحله جمع‌آوری تصاویر با نبودن مسیر app external files متوقف شد. نصب و smoke خروجی preview بعد از این مرحله قرار داشت و اجرا نشد؛ بنابراین APK هنوز تأیید تحویل ندارد.
- اصلاح نگهداری تصاویر پیش از پاک‌سازی package توسط ابزار تست ثبت شده، ولی نتیجه اجرای بعدی هنوز در دست نیست.

## منطق زمان و اعلان

تاریخ شمسی پس از validation به نمایش داخلی یکنواخت `yyyy/MM/dd` تبدیل می‌شود. اختلاف روزها روی تقویم مدنی UTC محاسبه می‌شود تا DST یا تغییر منطقه زمانی، یک روز را کم یا زیاد نکند. زمان واقعی reminder جداگانه به epoch در منطقه زمانی گوشی هنگام ثبت تبدیل می‌شود؛ سفر به منطقه دیگر policy نهایی ندارد و به‌عنوان محدودیت باز باقی است.

هر reminder رکورد مستقل دارد. مجوز Android، تنظیم داخلی و کانال پیش از مصرف آن بررسی می‌شوند. ثبت تکراری ساعت یکسان برای همان موعد رد می‌شود و مصرف مجدد همان رکورد با شرط اتمیک `fired_at IS NULL` جلوگیری می‌شود. پس از بازگشت برنامه، فقط آخرین مرحله ازدست‌رفته هر موعد در ۲۴ ساعت اخیر برای catch-up انتخاب می‌شود. این روش تضمین تحویل دقیق رأس ساعت یا تحویل پس از تمام شرایط خاموشی نیست؛ `setAndAllowWhileIdle` تابع محدودیت‌های Android است.
