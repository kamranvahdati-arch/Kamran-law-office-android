# ممیزی امضا و رسانه در ارتقای ۹.۱ به ۱۰

## مبنای قابل اثبات

- سورس شاخهٔ `codex/v091-deadlines-audit`: `release.signingConfig = signingConfigs.debug`؛ نسخهٔ `preview` نیز از همان release ارث می‌برد. workflow ۹.۱ هیچ کلید انتشار یا Secret امضا را نمی‌خواند.
- APK پیش‌نمایش ساخته‌شده در run 55، artifact `10630577977`: گواهی عمومی APK Signing Block با subject `CN=Android Debug`، SHA-256 گواهی `2ec6edf14c85b05fe3dbc5f1cf31cfe732d9d93bf30308f3c30146292e4107e4`.
- APK آزمایشی نسخهٔ ۱۰ ساخته‌شده در run 62: گواهی عمومی `CN=Android Debug`، SHA-256 گواهی `b456b271612dbf9cbe2f8433e50327a8535bfb00755f4892a967fca164d8dc7e`. اثر انگشت متفاوت است؛ جایگزینی مستقیم این دو APK، حتی با شناسهٔ بستهٔ یکسان، راه امن و قابل نصب نیست.
- در فایل‌های ردیابی‌شدهٔ ۹.۱ و سورس فعلی keystore پایدار وجود ندارد. ابزارهای متصل اجازهٔ خواندن نام یا مقدار Secretهای خصوصی تنظیمات GitHub را نمی‌دهند؛ نبودن هر کلید خارج از مخزن قابل اثبات نیست. هیچ کلید خصوصی ایجاد، اضافه یا منتشر نشده است.

## مرز واقعی انتقال رسانه

- ۹.۱ در `case_attachments.content_uri` فقط نشانی `ACTION_OPEN_DOCUMENT` و مجوز ماندگار **مختص UID همان بسته** را ثبت می‌کند؛ `.klo` فقط دیتابیس و بخشی از SharedPreferences را ذخیره می‌کند. عکس پروفایل و لوگوی انتخابی حتی در بخش پروفایل بکاپ قدیمی صادر نمی‌شدند.
- نصب هم‌نام روی همان UID و با امضای یکسان می‌تواند URI و فایل‌های قبلی را حفظ کند؛ اما APK توزیع‌شدهٔ ۹.۱ و APK آزمایشی ۱۰ اثر انگشت متفاوت دارند. پیش‌نمایش نیز پسوند `.preview` دارد و به‌هرحال با بستهٔ اصلی ۱۰ نصب هم‌نام نیست.
- نسخهٔ ۱۰ اکنون پیوست ناموجود را در صفحهٔ پرونده مشخص می‌کند. کاربر فایل اصلی را از انتخابگر Android دوباره می‌دهد؛ اپ نخست تمام بایت‌ها را در فضای خصوصی خود کپی می‌کند و تنها پس از موفقیت، URI رکورد را تغییر می‌دهد. نسخهٔ ۹.۱ و فایل اصلی هیچ‌گاه توسط این مسیر پاک نمی‌شوند. عکس پروفایل نیز هنگام انتخاب مجدد در فضای خصوصی کپی می‌شود.
- انتقال **خودکار همهٔ رسانه‌ها از بکاپ ۹.۱ ممکن نیست**: بایت‌ها در فایل قدیمی وجود ندارند و بستهٔ جدید مجوزهای URI بستهٔ قدیمی را به ارث نمی‌برد. بدون انتخاب دوبارهٔ فایل‌های اصلی توسط کاربر، ادعای ارتقای کامل ممنوع است. بکاپ فعلی نسخهٔ ۱۰ هم رسانه را ضمیمه نمی‌کند؛ نگهداری فایل‌های اصلی تا فراهم شدن بکاپ رسانه‌ای ضروری است.

## آزمون قابل تکرار

`SamePackageMediaUpgradeTest` روی شبیه‌ساز ۹.۱ پرونده، موکل، PDF و نشانی عکس را می‌سازد و بعد از `adb install -r` نسخهٔ ۱۰، بازشدن واقعی بایت‌های PDF و عکس و بقای دیتابیس را بررسی می‌کند. این مسیر تنها با کلید یکسان در همان اجرای CI معتبر است. `CrossPackageBackupTransferTest` بکاپ را از برنامهٔ واقعی شاخهٔ ۹.۱ پیش‌نمایش می‌گیرد، نشان می‌دهد URI خصوصی آن از بستهٔ ۱۰ باز نمی‌شود، سپس انتخاب مجدد فایل را در مسیر واقعی `onActivityResult` اجرا و بازشدن کپی خصوصی پس از حذف منبع انتخابی را بررسی می‌کند. موفقیت یا شکست اجرا باید از نتیجهٔ همان commit در GitHub Actions خوانده شود.
